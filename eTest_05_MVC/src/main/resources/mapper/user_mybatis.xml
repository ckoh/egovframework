<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
  <resultMap id="sample" type="user.vo.UserVO">
       <result property="phone" column="email"/>
       <result property="email" column="phone"/>
       <result property="userid" column="userid"/>
       <result property="username" column="username"/>
       <result property="userpwd" column="userpwd"/>
       <result property="address" column="address"/>
  </resultMap>
  <insert id="add"  parameterType="user.vo.UserVO">
		insert into userinfo1 
		(userid, username, userpwd, email, phone,address)
		values 
		(#{userid}, #{username}, #{userpwd},
		 #{email},#{phone},#{address})
	</insert>
	<select id="login" resultType="user.vo.UserVO" 
	                   parameterType="user.vo.UserVO">
		select * from userinfo 
		where userid=#{userid} and userpwd = #{userpwd}
	</select>
	<select id="getuser" resultType="user.vo.UserVO">
		select * from userinfo 
		where userid = #{userid}
	</select>
	<select id="list" resultType="user.vo.UserVO">
		select * from userinfo1
	</select>
	<update id="update" parameterType="user.vo.UserVO">
		update userinfo 
		set email=#{email},  phone=#{phone}, address=#{address} 
		where userid = #{userid}
	</update>
	<delete id="delete">
		delete from userinfo where userid = #{userid}
	</delete>
	
	<select id="find" resultType="user.vo.UserVO"
	                  parameterType="hashMap">
		select * from userinfo 
		<where>
           <if test="username != null">
                username like  '%'|| #{username} ||'%'
           </if>	
           <if test="userid != null">
                userid like  '%'|| #{userid} ||'%'
           </if>
           <if test="email != null">
                email like  '%'|| #{email} ||'%'
           </if>
		</where>
	</select>
</mapper>






